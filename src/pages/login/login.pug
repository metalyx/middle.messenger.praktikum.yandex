section.login-page
  div.login-page-wrapper
    div.title Вход
    main
      form(name='loginForm', id='login-form', onsubmit="validateMyForm();")
        div.inputs
          //- input.input(placeholder='Логин' type='text' name='login' id='login')
          //- input.input(placeholder='Пароль' type='password' name='password' id='password')
          each val in inputs
            != val
        div.buttons
          button.button(type='button' id='submit-login')
            a(href='main.pug') Авторизоваться
          div.no-profile
            a(href='') Нет аккаунта?
